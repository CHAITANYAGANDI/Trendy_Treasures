# Local development value. Production is configured in Vercel:
# REACT_APP_AUTH_URL=https://trendy-auth-ppa6nvipwa-pd.a.run.app/auth
# React env vars are build-time; redeploy Vercel after changing production values.

# Public URL of the Auth/server, including the /auth path prefix.
REACT_APP_AUTH_URL=http://localhost:5000/auth
